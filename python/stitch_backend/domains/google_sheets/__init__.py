"""Google Sheets domain — core-only OAuth flow (google_oauth_* commands).

The Identity Graph CRUD commands are served by the ``stitch-sheets``
plugin via the dual route (see ``plugin_runtime/sheets_dual.py``).
"""
